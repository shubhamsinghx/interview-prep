# MaxStack

A stack data structure that supports retrieving the current maximum element in O(1) time.

## Operations

- `push(value)` — pushes a value onto the stack
- `pop()` — removes and returns the top element
- `currentMax()` — returns the current maximum element in the stack

## Run Tests

```bash
npm install
npm test
```
